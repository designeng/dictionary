define [
    "react"
    "reactRouter"
], (React, Router) ->

    Inbox = React.createClass

        render: ->
            return (
                <div>
                    <h1>Inbox!</h1>
                </div>
            )